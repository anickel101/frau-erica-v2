# Database (existing)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# DBeaver workspace/project files
.dbeaver/
*.dbeaver-data-sources.xml

# App (React/Vite site)
node_modules
dist
dist-ssr
*.local
.env
.env.local
*.tsbuildinfo

# API (Lambda/SAM)
.aws-sam/

# Editor -- explicitly scoped to app/, since .vscode/* alone won't
# reach into a nested folder from the root .gitignore
app/.vscode/*
!app/.vscode/extensions.json
!app/.vscode/settings.json
.idea

# OS
.DS_Store

